import { Account } from './accounts'
import { Rules } from './rules'
import { DID } from './dids'
import { Auth } from './auth'

export const RucioClient = {
    Account,
    Auth,
    Rules,
    DID,
}
